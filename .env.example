# Base URL for form submissions (enquiry, contact, newsletter).
# Leave unset in development: submissions resolve locally and log the payload
# instead of hitting the network. See src/utils/api.js.
VITE_API_BASE_URL=
